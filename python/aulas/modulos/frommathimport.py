from math import sqrt, floor #se apertar crtl + espaço, aparece todos 
num = int(input('Digite um número '))
raiz = sqrt(num)
print('A raiz de {} é igual a {}' .format(num, floor(raiz)))
# para importar biblioteca, basta ir no python.org, docs e escolher a biblioteca. ou, clique em pypi, para importar pacotes separados